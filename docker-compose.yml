services:
  mongodb:
    image: mongo:7
    container_name: mockpulse-mongodb
    restart: unless-stopped
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: mockpulse
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
